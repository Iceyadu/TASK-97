# Application source (`src/`)

Project documentation, Docker Compose, and the main **README** live at the **repository root**:

- [`../README.md`](../README.md) — full guide (Quick Start, testing, deployment)
- [`../docker-compose.yml`](../docker-compose.yml) — Compose stack
- [`../run_tests.sh`](../run_tests.sh) — Docker test runner

NestJS code and `package.json` are in this directory.
